# ESV API – required for Bible reader. Get a free key at https://api.esv.org/account/
# Set on the server only (Vercel project env, or .env when running `vercel dev`). Do not use VITE_ prefix.
ESV_API_KEY=your_key_here

# Optional: donations
# External donations link (Venmo, Ko-fi, PayPal, etc.). Button label is derived from URL. Shown on Donations page. Use VITE_ prefix.
# VITE_SUPPORT_URL=https://account.venmo.com/u/yourusername

# Set to "true" only if you use Stripe on the Donations page; otherwise the one-time payment section is hidden. Use VITE_ prefix.
# VITE_SUPPORT_STRIPE_ENABLED=true

# Stripe – for one-time payments on /donate. Server only (no VITE_ prefix). Requires VITE_SUPPORT_STRIPE_ENABLED=true.
# Get keys at https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_test_...
