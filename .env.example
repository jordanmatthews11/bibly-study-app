# ESV API – required for Bible reader. Get a free key at https://api.esv.org/account/
# Set on the server only (Vercel project env, or .env when running `vercel dev`). Do not use VITE_ prefix.
ESV_API_KEY=your_key_here

# Optional: donations/support
# External support link (Ko-fi, Buy Me a Coffee, PayPal.me). Shown on /donate. Use VITE_ prefix so the client can read it.
# VITE_SUPPORT_URL=https://ko-fi.com/yourpage

# Stripe – for one-time payments on /donate. Server only (no VITE_ prefix).
# Get keys at https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_test_...
